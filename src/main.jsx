import React from "react";
import ReactDOM from "react-dom/client";
import AICockpit from "./bomber.jsx";
import "./index.css"; // optional, falls du Tailwind oder eigenes CSS willst

ReactDOM.createRoot(document.getElementById("root")).render(<AICockpit />);
